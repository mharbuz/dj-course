package config

const (
	VEHICLES              = 50
	DRIVERS               = 20
	TRANSPORTATION_ORDERS = 1000
	CUSTOMERS             = 500

	// Availability: slots per resource (driver/vehicle), and how many days ahead to generate.
	AVAILABILITY_SLOTS_PER_RESOURCE = 5
	AVAILABILITY_DAYS_AHEAD         = 30
)
